# convertsite-prototypes
A collection of Michels prototypes, built when Figma doesn't suffice. Each folder contains a separate prototype.

# Test in browser
- Some prototypes need JS, use something like HTTP server (Install: npm install --global http-server or brew install http-server → Run: http-server) to run a simple server
- Or just open the index.html file in your browser for the simple proto's